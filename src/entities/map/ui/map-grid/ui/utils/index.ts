import { Map } from '../../../../model/types';

export const getStartMap = (rowAmount: number, colAmount: number): Map => {
  let map = {} as Map;

  [...new Array(rowAmount)].forEach((r, rowIdx) => {
    [...new Array(colAmount)].forEach((c, colIdx) => {
      map[]
    
    });
  });


  return map
}
